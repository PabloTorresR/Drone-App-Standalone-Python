"""Type declarations for common objects in the app"""
from typing import Tuple

# Types related to Position and Movement
Position = Tuple[int, int, int]
Movement = Tuple[int, int, int]
World = Tuple[int, int, int]
